// pageDetailsProduict.js
window.addEventListener("DOMContentLoaded", function () {
  // const urlParams = new URLSearchParams(window.location.search);
  // const serviceId = urlParams.get("id");
  // if (serviceId) {
  //   fetchServiceDetails(serviceId);
  // } else {
  //   console.error("No service ID found in URL");
  // }
  // async function fetchServiceDetails(id) {
  //   try {
  //     const response = await fetch(`${BASE_URL}/service/${id}`, {
  //       method: "GET",
  //     });
  //     const result = await response.json();
  //     console.log("result", result);
  //     if (response.ok) {
  //       renderServiceDetails(result.data);
  //     } else {
  //       console.error(result.message);
  //     }
  //   } catch (error) {
  //     console.log(error);
  //   }
  // }
  // function renderServiceDetails(service) {
  //   const serviceContainer = document.getElementById("service-details");
  //   if (serviceContainer) {
  //     serviceContainer.innerHTML = `
  //       <h1>${service.name}</h1>
  //       <img src="${service.images[0]}" alt="${service.name}" />
  //       <p>${service.description}</p>
  //       <p>Price: ${service.price} FCFA</p>
  //       <p>Artisant: ${service.artisant.user.firstname} ${service.artisant.user.lastname}</p>
  //       <p>Job: ${service.artisant.job}</p>
  //     `;
  //   }
  // }
});
